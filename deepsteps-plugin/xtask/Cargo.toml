[package]
name = "xtask"
version = "0.1.0"
edition = "2021"
description = "A helper for compiling and bundling the DeepSteps plugin, invoked via `cargo xtask`"

[dependencies]
nih_plug_xtask = { git = "https://github.com/robbert-vdh/nih-plug.git" }
